import React from "react";
import { Text, View, StatusBar } from "react-native";

const Settings = () => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
}

export default Settings;